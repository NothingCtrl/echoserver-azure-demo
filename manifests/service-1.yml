apiVersion: v1
kind: Service
metadata:
    name: "rangotest-294c"
    labels:
        app: "rangotest-294c"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "rangotest-294c"